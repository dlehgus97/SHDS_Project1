<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.nextus.admin.AdminMapper">
		<select id="Listview" resultType="kr.co.nextus.admin.AdminVO">
			SELECT no, title, content, writedate
			FROM NOTICE	    
		</select>
		
	    <delete id="deleteNotice" parameterType="int">
	        DELETE FROM NOTICE
	        WHERE no = #{no}
    	</delete>
    	
    	<insert id="insertnotice">
	        INSERT INTO NOTICE (no, title, content, writedate)
	        VALUES ((SELECT COALESCE(MAX(no), 0) + 1 FROM NOTICE), #{title}, #{content}, NOW())<!-- 이부분 수정 -->
	    </insert>
    			
</mapper>   