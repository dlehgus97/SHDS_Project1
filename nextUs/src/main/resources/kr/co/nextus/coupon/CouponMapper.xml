<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.nextus.coupon.CouponMapper">



	<!-- ========== -->
	<!-- 관리자 페이지 -->
	<!-- ========== -->
	<insert id="create" parameterType="kr.co.nextus.coupon.CouponVO">
		INSERT INTO COUPON (
		no,memberno,discount,status,name,type
		)VALUES (
		#{no},3,#{discount},1,#{name},#{type});
		<selectKey order="AFTER" keyProperty="no" resultType="int">
			SELECT
			LAST_INSERT_ID()
		</selectKey>
	</insert>
<!--  #{memberno} -->

	<!-- 리스트 -->
	<select id="list" parameterType="kr.co.nextus.coupon.CouponVO"
		resultType="kr.co.nextus.coupon.CouponVO">
		SELECT * FROM COUPON
		ORDER BY no
		LIMIT #{startIdx}, 10
	</select>
	<!-- 카운트 -->
	<select id="count" parameterType="kr.co.nextus.coupon.CouponVO"
		resultType="int">
		SELECT COUNT(*) FROM COUPON
	</select>


</mapper>