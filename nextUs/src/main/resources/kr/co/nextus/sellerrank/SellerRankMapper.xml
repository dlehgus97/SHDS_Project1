<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.nextus.sellerrank.SellerRankMapper">
	<!-- 목록 -->
	<select id="list" parameterType="kr.co.nextus.sellerrank.SellerRankVO" resultType="kr.co.nextus.sellerrank.SellerRankVO">
		SELECT *,
		(SELECT nested FROM CATEGORYLIST WHERE categoryno=category1) as nested1,
		(SELECT nested FROM CATEGORYLIST WHERE categoryno=category2) as nested2,
		(SELECT COUNT(*) FROM BUYLIST WHERE memberno=category2) as sellerrank
		FROM SELLER 
		JOIN MEMBER ON SELLER.no=MEMBER.no
		ORDER BY sellerrank DESC
    	LIMIT 10;
	</select>

</mapper>   